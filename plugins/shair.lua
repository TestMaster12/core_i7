--shared by @blackhatchannel

do
function run(msg, matches)
  if not is_sudo(msg) then
    return "Only sudoers can give bot account "
    end
function run(msg, matches)
send_contact(get_receiver(msg), "Your Bot Phone Number", "fri name", "Las name", ok_cb, false)
end

return {
patterns = {
"^!share$",
"^share$"

},
run = run
}

end
--shared by @blackhatchannel
