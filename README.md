# core_i7
```bash
sudo apt-get update && sudo apt-get upgrade && sudo apt-get install libreadline-dev libconfig-dev libssl-dev lua5.2 liblua5.2-dev libevent-dev make unzip git redis-server g++ libjansson-dev libpython-dev expat libexpat1-dev && cd $home &&  git clone https://github.com/Lenovocorei7/core_i7.git && cd core_i7 && chmod +x launch.sh && ./launch.sh install && ./launch.sh
